# Example hunt queries

Copy any file here to `45_hunt_queries/<your-slug>.md`, replace placeholders, and add a row to [`../INDEX.md`](../INDEX.md).

Do **not** set `related_samples` to real slots until you have run the query in a hunt engagement.
