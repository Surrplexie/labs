# 45_hunt_queries (this lab)

Queries you actually ran for **this** hunt. Reusable library lives at repo root:

`..\..\45_hunt_queries\`

```powershell
powershell -ExecutionPolicy Bypass -File .\30_scripts\new_hunt_query.ps1 -Slug my-query
```
