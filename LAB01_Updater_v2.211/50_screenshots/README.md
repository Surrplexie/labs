# 50_screenshots

Drop evidence **here** (this lab only). Keep `SHOT_INDEX.txt` in sync.

From repo root, before commit:

```powershell
powershell -ExecutionPolicy Bypass -File .\30_scripts\strip-exif.ps1 -SampleId sample_01
```

