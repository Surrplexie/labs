# 50_screenshots

Drop screenshots **here** (this lab only). Keep `SHOT_INDEX.txt` in sync.

From repo root, before commit:

```powershell
powershell -ExecutionPolicy Bypass -File .\30_scripts\strip-exif.ps1 -SampleId sample_02
```

Recordings / slides / exports go in sibling `55_media/` (local; binaries are gitignored).

