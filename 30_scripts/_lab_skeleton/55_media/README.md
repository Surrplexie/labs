# 55_media

Local drop for **this** lab: recordings, lecture slides, exports, anything that is not a screenshot.

| Put here | Do not commit (gitignored) |
|----------|----------------------------|
| `MEDIA_INDEX.md` — what the file is | `.mp4` `.mkv` `.mov` `.webm` `.wav` `.mp3` `.pdf` |

Screenshots still go in `50_screenshots/` (those can be committed after EXIF strip).

Class slides that belong with coursework stay in `Downloads\Notes\<course>\`.
