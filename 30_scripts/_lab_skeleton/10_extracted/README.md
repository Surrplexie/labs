# 10_extracted

File-kind only. Non-executable extracts (strings, NSIS script, manifests).

No `.exe`, `.dll`, `.ps1`, shellcode, or dumps.

Name files with the slot prefix, e.g. `SAMPLE_ID_strings.txt`.
