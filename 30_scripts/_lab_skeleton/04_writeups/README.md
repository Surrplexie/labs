# 04_writeups

Optional long-form report. `03_findings` is still the required close artifact.

Scaffold from repo root:

```powershell
powershell -ExecutionPolicy Bypass -File .\30_scripts\scaffold_writeup.ps1 -SampleId SAMPLE_ID
```

Templates live at repo `04_writeups/_templates/` (shared, not copied per lab).
